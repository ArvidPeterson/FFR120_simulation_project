
from SuperAgent import SuperAgent

class Bird(SuperAgent):
     def __init__(self, x, y):
         super().__init__(self, x, y)
         self.x_nest
         self.y_nest
         self.is_in_nest
         self.nest
         self.has_nest = False
         self.x = x
         self.y = y

